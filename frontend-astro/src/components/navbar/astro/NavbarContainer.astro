---

---

<header class="fixed top-0 left-0 right-0 z-50">
    <nav class="glass-effect border-b border-purple-500/10">
      <div class="container max-w-7xl mx-auto px-4 md:px-8 lg:px-16">
        <slot />
      </div>
    </nav>
  </header>
  
  <style>
    .glass-effect {
      background: rgba(23, 23, 35, 0.7);
      backdrop-filter: blur(10px);
    }
  </style>